services:
  sentinel-listener:
    build: .
    container_name: sentinel-listener
    restart: unless-stopped
    working_dir: /app
    env_file:
      - .env
    environment:
      - WORKSPACE=/app
      - GITHUB_WORKSPACE=/app
    volumes:
      - ./:/app
    command: bash commands/listen.sh
